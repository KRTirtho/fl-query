name: fl_query_hooks_example
description: A new Flutter project.

version: 1.0.0+1

publish_to: none

environment:
  sdk: ">=2.19.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  fl_query:
    path: ../../fl_query
  fl_query_hooks:
    path: ../
  go_router: ^6.0.9
  http: ^0.13.5
  flutter_hooks: ^0.18.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
